import { ProjectList } from "./ProjectsList";

export const ProjectsSection = () => {
    return(
        <section>
            <h2>Projetos</h2>

            <ProjectList />
        </section>
    )
};
